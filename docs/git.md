# git

```bash
$ git config --global user.name "Ash Williams <ash@s-mart.biz>"
$ git config --global user.email "ash@s-mart.biz"
```

```bash
alias ga="git add"
alias gc="git commit"
alias gcm="git commit -m"
alias gs="git status"
```
